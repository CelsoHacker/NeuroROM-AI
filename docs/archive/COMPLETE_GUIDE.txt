# 🚀 GUIA COMPLETO - Tradutor Universal v5.0

## 📋 3 MODOS DISPONÍVEIS

| Modo | Velocidade | Temperatura | Custo | Qualidade |
|------|-----------|-------------|-------|-----------|
| **Offline (Gemma)** | 20-30 textos/s | ~70°C | Grátis | Boa |
| **Online (Gemini)** | 100-200 textos/s | 0°C | Grátis* | Ótima |
| **Online (DeepL)** | 50-100 textos/s | 0°C | Pago | Excelente |

*Gemini: 60 req/min grátis

---

## ⚙️ CONFIGURAÇÃO INICIAL

### **1. Instale Gemma 2B (Offline Rápido)**

```bash
# Mais leve que llama3.1 (1.9GB vs 4.7GB)
ollama pull gemma:2b

# Teste
ollama run gemma:2b "Hello"
```

**Vantagens:**
- ✅ 70°C (vs 80°C do llama3.1)
- ✅ 2GB VRAM (vs 4-5GB)
- ✅ Mais rápido
- ✅ Grátis

---

### **2. Configure Gemini API (Online GRÁTIS)**

#### **A. Obtenha API Key:**
1. Acesse: https://makersuite.google.com/app/apikey
2. Clique em **"Create API Key"**
3. Copie a key (começa com `AIza...`)

#### **B. Configure no Script:**

Edite `tradutor_universal.py` **linha 67**:

```python
GEMINI_API_KEY = "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXX"  # Sua key aqui
```

**Ou** passe via argumento:
```bash
python tradutor_universal.py teste.txt --mode gemini --gemini-key "SUA_KEY"
```

**Limites Grátis:**
- 60 requisições/minuto
- ~3600/hora
- Suficiente para 100k+ textos/dia

---

### **3. Configure DeepL API (Opcional - PAGO)**

#### **A. Crie Conta:**
1. Acesse: https://www.deepl.com/pro-api
2. Escolha plano **Free** (500k chars/mês)
3. Requer cartão de crédito (não cobra se não exceder)

#### **B. Configure:**

Edite `tradutor_universal.py` **linha 73**:

```python
DEEPL_API_KEY = "sua-key-aqui:fx"
```

---

## 🎯 COMO USAR

### **Modo 1: Offline (Gemma 2B)**

```bash
# Básico
python tradutor_universal.py textos_para_traduzir.txt

# Com saída customizada
python tradutor_universal.py textos_para_traduzir.txt game_traduzido.txt

# Especificando modo
python tradutor_universal.py textos.txt --mode offline
```

**Características:**
- Temperatura: ~70°C
- Velocidade: 20-30 textos/s
- Tempo (41k textos): ~25-30 min
- **Sem internet necessária**

---

### **Modo 2: Online Gemini (RECOMENDADO)**

```bash
# Com key no arquivo
python tradutor_universal.py textos.txt --mode gemini

# Com key na linha de comando
python tradutor_universal.py textos.txt traduzido.txt \
  --mode gemini \
  --gemini-key "AIzaSyXXXXXXXXXXXXXX"
```

**Características:**
- Temperatura: 0°C (não usa GPU)
- Velocidade: 100-200 textos/s
- Tempo (41k textos): **5-10 min** ⚡
- **GRÁTIS**

---

### **Modo 3: Online DeepL**

```bash
python tradutor_universal.py textos.txt --mode deepl --deepl-key "sua-key:fx"
```

**Características:**
- Melhor qualidade
- 50-100 textos/s
- Tempo (41k textos): 10-15 min
- Limita por caracteres (não por textos)

---

## 📊 COMPARAÇÃO DE DESEMPENHO

### **Teste: 41.027 textos do Legend of game**

| Modo | Tempo | Taxa | Temperatura | Custo |
|------|-------|------|-------------|-------|
| llama3.1 (antigo) | 2h | 5-10/s | 80°C | Grátis |
| **Gemma 2B** | 25 min | 25-30/s | 70°C | Grátis |
| **Gemini** | **8 min** | 85/s | 0°C | Grátis |
| DeepL | 12 min | 57/s | 0°C | $0 (free tier) |

---

## 🎮 JOGOS SNES (Pequenos)

Para jogos SNES típicos (500-2000 linhas):

```bash
# Offline (rápido o suficiente)
python tradutor_universal.py chrono_trigger.txt --mode offline

# Tempo: ~1-3 minutos
```

**Recomendação SNES:**
- Use **Offline (Gemma)** - suficientemente rápido
- Ou **Gemini** se quiser instantâneo (10-30s)

---

## 🔧 TROUBLESHOOTING

### **Erro: "GEMINI_API_KEY não configurada"**

**Solução:**
```bash
# Opção 1: Edite o arquivo linha 67
GEMINI_API_KEY = "sua-key"

# Opção 2: Passe via argumento
--gemini-key "sua-key"
```

---

### **Erro: "Ollama não está rodando"**

**Solução (Modo Offline):**
```bash
# Terminal 1
ollama serve

# Terminal 2
python tradutor_universal.py textos.txt
```

---

### **Gemini Erro 429 (Rate Limit)**

**Causa:** Excedeu 60 req/min

**Solução:**
```python
# Edite linha 71 do tradutor_universal.py
MAX_WORKERS_GEMINI = 5  # Era 10, reduza para 5
```

---

### **GPU Ainda Esquentando (Offline)**

**Soluções:**
1. **Reduza Power Limit:**
```bash
nvidia-smi -pl 90  # Limita a 90W
```

2. **Use Apenas 1 Worker:**
```python
# Linha 69
MAX_WORKERS_OFFLINE = 1
```

3. **Melhore Ventilação:**
- Limpe GPU
- Aumente RPM das ventoinhas
- Melhore airflow do gabinete

---

## 💡 DICAS PRO

### **1. Combine Cache + API Online**

O cache funciona entre modos! Traduza parcialmente offline, depois continue com Gemini:

```bash
# 1. Traduza 1000 textos offline (testa + constrói cache)
python tradutor_universal.py teste_1000.txt --mode offline

# 2. Continue com arquivo completo usando Gemini (cache pula os 1000)
python tradutor_universal.py textos_completo.txt --mode gemini
```

---

### **2. Batch por Jogo**

Para múltiplos jogos SNES:

```bash
# Loop batch
for game in chrono_trigger final_fantasy zelda; do
    python tradutor_universal.py ${game}.txt --mode gemini
done
```

---

### **3. Monitore Temperatura em Tempo Real**

```powershell
# PowerShell
while ($true) {
    nvidia-smi --query-gpu=temperature.gpu --format=csv,noheader
    Start-Sleep -Seconds 2
}
```

Pare se passar de 75°C!

---

## 📁 ARQUIVOS GERADOS

```
projeto/
├── tradutor_universal.py          # Script principal
├── cache_traducoes.json            # Cache (compatível entre modos)
├── translator_errors.log           # Log de erros
├── textos_para_traduzir.txt        # Entrada
└── textos_traduzidos.txt           # Saída
```

---

## ✅ CHECKLIST DE USO

### **Primeira Vez:**
- [ ] Instalou Gemma: `ollama pull gemma:2b`
- [ ] Obteve Gemini API key
- [ ] Configurou key no script
- [ ] Testou com arquivo pequeno (100 linhas)

### **Para Cada Tradução:**
- [ ] Escolheu o modo (offline/gemini/deepl)
- [ ] Verificou temperatura (offline) ou API key (online)
- [ ] Executou o comando
- [ ] Verificou o arquivo de saída

---

## 🎯 RESUMO - QUAL USAR?

| Situação | Modo Recomendado |
|----------|------------------|
| **Sem internet** | Offline (Gemma) |
| **Máxima velocidade** | Gemini API |
| **GPU esquentando** | Gemini API |
| **Melhor qualidade** | DeepL API |
| **Jogos SNES** | Offline ou Gemini |
| **Jogos PS1 grandes** | Gemini API |
| **Teste inicial** | Offline (100 linhas) |

---

## 📞 SUPORTE

**Problemas?**
1. Verifique `translator_errors.log`
2. Teste com arquivo pequeno primeiro
3. Confirme API keys (se online)
4. Monitore temperatura (se offline)

**Performance ruim?**
- Offline: Reduza workers para 1
- Gemini: Verifique rate limits (60/min)
- DeepL: Verifique cota de caracteres

---

🚀 **PRONTO PARA USAR!**

Comece com `gemini` para velocidade máxima ou `offline` se preferir local.